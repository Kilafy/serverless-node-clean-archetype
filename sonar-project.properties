# Meta-información del proyecto
sonar.projectKey=lambda_archetype
sonar.projectName=lambda_archetype
sonar.projectVersion=1.0

# Fuente y lenguaje
sonar.sources=src
sonar.language=ts
sonar.sourceEncoding=UTF-8

# Excluir archivos o carpetas de la revisión (opcional)
sonar.exclusions=**/node_modules/**,**/tests/**

# Especificaciones para TypeScript (opcional)
sonar.typescript.lcov.reportPaths=src/coverage/lcov.info

# Habilitar reglas de ESLint (opcional)
sonar.typescript.eslint.reportPaths=src/eslint-report.json

# Información para el análisis de cobertura (opcional)
sonar.test.inclusions=**/*.spec.ts, **/*.test.ts
